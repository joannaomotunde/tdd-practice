# TDD Practice Project

This project implements functions with test-driven development using Vitest.

## Functions

1. add(a, b)
2. subtract(a, b)
3. multiply(a, b)
4. divide(a, b)
5. reverseString(str)
6. isPalindrome(str)
7. findMax(arr)

## How to run tests

```bash
npm install
npm test
